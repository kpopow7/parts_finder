services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: shade
      POSTGRES_PASSWORD: shade
      POSTGRES_DB: shade_catalog
    ports:
      - "5432:5432"
    volumes:
      - shade_pg_data:/var/lib/postgresql/data

volumes:
  shade_pg_data:
