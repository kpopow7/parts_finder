# Shade Product Catalog Python dependencies
# Install: pip install -r requirements.txt
#
# Note: The app also requires external programs/services not installable via pip:
# - PostgreSQL (or Docker Desktop to run the provided docker-compose database)

fastapi>=0.115.0
python-multipart>=0.0.9
uvicorn[standard]>=0.32.0
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
psycopg[binary]>=3.2.0
alembic>=1.14.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
pypdf>=5.4.0

# Dev/test/lint tooling used in this repo
pytest==8.4.2
httpx==0.28.1
ruff==0.12.8
